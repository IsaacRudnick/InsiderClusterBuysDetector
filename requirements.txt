# Core data stack — used by every module.
pandas>=2.2
numpy>=1.26
pyarrow>=15.0

# SEC scraping + price data.
requests>=2.31
yfinance>=0.2.40

# Reporting.
plotly>=5.20
openpyxl>=3.1

# Ranking model (backtest/research.py, research/model.py).
scikit-learn>=1.4
lightgbm>=4.3
# shap gives true Shapley interaction values, which the 10-percent-owner
# hypothesis test needs. Without it the code falls back to a weaker proxy
# built on lightgbm pred_contrib. Installing shap pulls in numba, which
# pins numpy below the newest release. Verified working on numpy 2.4.6.
shap>=0.44
